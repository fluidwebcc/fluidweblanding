name = "fluidweb"
compatibility_date = "2026-09-13"

# Client-side routes (/work, /team, /faq) must serve the SPA, not a raw 404.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
